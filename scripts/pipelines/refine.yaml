# Refine Pipeline
# Improves plan, then refines tasks

name: refine
description: Complete planning refinement - plan first, then tasks

nodes:
  - id: improve-plan
    stage: improve-plan
    runs: 5

  - id: refine-tasks
    stage: refine-tasks
    runs: 5
    inputs:
      from: improve-plan
      select: latest
