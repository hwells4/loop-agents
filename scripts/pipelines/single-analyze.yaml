name: single-analyze
description: Single provider test

nodes:
  - id: analyze
    parallel:
      providers: [claude]
      stages:
        - name: improve
          stage: improve-plan
          termination:
            type: fixed
            iterations: 1
