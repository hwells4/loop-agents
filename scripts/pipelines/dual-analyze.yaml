name: dual-analyze
description: Parallel analysis with Claude Opus and Codex

nodes:
  - id: analyze
    parallel:
      providers: [claude, codex]
      stages:
        - name: improve
          stage: improve-plan
          termination:
            type: fixed
            iterations: 1
        - name: elegance
          stage: elegance
          termination:
            type: fixed
            iterations: 1
