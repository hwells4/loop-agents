# Create Tasks Stage - Generate beads from a plan document
# Autonomous version for pipeline use (no user interaction)

name: create-tasks
description: Generate executable beads from a plan document

termination:
  type: fixed
  iterations: 1

delay: 1
